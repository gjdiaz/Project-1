// The Project Add/Edit button populates the row of data for each Project in the table
// client-side storage (Activity 23 local storage) function

// Search button with text area, searches for xyz
// For loop function to make text input searchable
// create variable for each: Project Name, Project#, Start/Finish Dates, Job Walk, Status
// for invalid text input - rock paper scissors example (Activity 25):
/* var validChoices = [x, y, z];
 if (!validChoices.includes(user.choice))  {
    //inform user of invalid choice using modal or tooltip
}
*/

// Create a modal for when Project Add/Edit is clicked on
// Data can be entered in modal for Project Name, Project#, Start/Finish Dates, Job Walk, Status, Comments
// modal function

// Status is colorcoded
// Function for if a project is overdue between 12 and 36 hours = green, between 36 and 48 hours = yellow, over 48 hours = red
// moment.js function to synch projects with real-time
// if then statements for color coding

// Make Job Walk and Status buttons sortable

// Click on row and a modal pops up with Address and Comments
// modal function and event delegation (Activity 19 HTML Attributes)

// Address in modal populates from googlemaps API

// Click on Weather button to show local (San Antonio) weather - need API

